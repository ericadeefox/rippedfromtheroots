# Template file for environment variables; use for creating .env, .env.dev, and .env.production

export APP_NAME = "ericadeefox"
export DEBUG = true
export SECURITY_SALT = null
export RESET_SALT = null
export PROFILE_SALT = null

# Database
export DB_DATABASE = "okbvtfr_ericadeefox"
export DB_USERNAME = "root"
export DB_PASSWORD = null

# Cookies
export COOKIE_ENCRYPTION_KEY = null
